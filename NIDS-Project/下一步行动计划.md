# 🎯 NIDS项目下一步行动计划

**更新时间**：2026年4月16日  
**当前阶段**：Day 4-7（模型训练与功能开发）  
**整体进度**：21.4% → 目标 50%

---

## 📊 当前状态总结

### ✅ 已完成（Day 1-3）
1. ✅ 开发环境配置（TensorFlow 2.21.0）
2. ✅ NSL-KDD数据集准备与预处理
3. ✅ RandomForest基线模型训练
4. ✅ 阈值优化（最优值：0.235）
5. ✅ 项目完成度报告生成
6. ✅ 代码同步到Gitee

### 🔄 进行中
- 🔄 CNN-LSTM模型训练（后台运行中）

### ⏳ 待完成（高优先级）
1. ⏳ PCAP分析功能开发
2. ⏳ 特征工程模块完善
3. ⏳ 前端优化与集成
4. ⏳ 系统集成测试

---

## 🎯 Day 4-7 详细行动计划（4月19-22日）

### 第一优先级：模型训练与优化

#### 1. CNN-LSTM模型训练 🔄
**负责人**：黄子权  
**状态**：进行中（后台训练）  
**预计完成时间**：今天晚上

**任务清单**：
- [x] 修改训练脚本适配NSL-KDD数据集
- [x] 启动CNN-LSTM模型训练
- [ ] 监控训练过程（准确率、损失值）
- [ ] 保存最佳模型权重
- [ ] 生成训练报告

**验收标准**：
- 训练准确率 > 85%
- 测试准确率 > 80%
- 召回率 > 75%
- F1-score > 80%

**输出文件**：
```
backend/backend/model/trained/cnn_lstm_model.h5
backend/reports/cnn_lstm_training_report.txt
backend/reports/cnn_lstm_confusion_matrix.png
```

---

#### 2. 模型融合（Ensemble）
**负责人**：黄子权  
**状态**：待开始  
**预计开始时间**：CNN-LSTM训练完成后  
**预计工作量**：4小时

**任务清单**：
- [ ] 读取已训练的RandomForest模型
- [ ] 读取已训练的CNN-LSTM模型
- [ ] 实现投票融合策略（Voting）
- [ ] 实现加权融合策略（Weighted Average）
- [ ] 对比融合模型与单模型性能
- [ ] 选择最佳融合策略

**实现方案**：
```python
# 伪代码
predictions_rf = rf_model.predict_proba(X_test)
predictions_cnn = cnn_lstm_model.predict(X_test)

# 加权融合
ensemble_pred = 0.4 * predictions_rf + 0.6 * predictions_cnn
```

**验收标准**：
- 融合模型准确率 > 单模型准确率
- 融合模型F1-score > 82%

**输出文件**：
```
backend/train_ensemble.py
backend/backend/model/trained/ensemble_model.pkl
backend/reports/ensemble_comparison.txt
```

---

### 第二优先级：PCAP分析功能

#### 3. PCAP特征提取实现
**负责人**：张子城  
**状态**：待开始  
**预计工作量**：8小时  
**截止时间**：4月20日

**任务清单**：
- [ ] 安装scapy库（`pip install scapy`）
- [ ] 实现PCAP文件读取功能
- [ ] 提取基础特征（协议类型、端口、IP地址）
- [ ] 提取统计特征（包大小、时间间隔）
- [ ] 提取流特征（会话统计）
- [ ] 特征标准化（使用训练时的scaler）
- [ ] 单元测试

**实现方案**：
```python
# backend/pcap_analyzer.py
from scapy.all import rdpcap, IP, TCP, UDP
import numpy as np

class PcapAnalyzer:
    def __init__(self, model_path, scaler_path):
        self.model = load_model(model_path)
        self.scaler = load_scaler(scaler_path)
    
    def extract_features(self, pcap_file):
        packets = rdpcap(pcap_file)
        features = []
        # 提取41维特征
        return np.array(features)
    
    def predict(self, pcap_file):
        features = self.extract_features(pcap_file)
        features_scaled = self.scaler.transform(features)
        predictions = self.model.predict(features_scaled)
        return predictions
```

**验收标准**：
- 能够成功读取PCAP文件
- 提取的特征维度与训练数据一致（41维）
- 特征值范围合理
- 预测结果准确

**输出文件**：
```
backend/pcap_analyzer.py
backend/tests/test_pcap_analyzer.py
backend/tests/sample.pcap（测试文件）
```

---

#### 4. PCAP分析API接口
**负责人**：张子城  
**状态**：待开始  
**依赖**：PCAP特征提取完成  
**预计工作量**：4小时

**任务清单**：
- [ ] 在Flask中添加`/api/analyze_pcap`端点
- [ ] 实现文件上传处理
- [ ] 调用PcapAnalyzer进行分析
- [ ] 生成分析报告（JSON格式）
- [ ] 错误处理与日志记录
- [ ] API测试

**API设计**：
```python
@app.route('/api/analyze_pcap', methods=['POST'])
def analyze_pcap():
    """
    接收PCAP文件，返回分析结果
    
    Request:
        - file: PCAP文件（multipart/form-data）
    
    Response:
        {
            "status": "success",
            "total_packets": 1000,
            "normal_packets": 800,
            "attack_packets": 200,
            "attack_rate": 0.20,
            "details": [
                {"packet_id": 1, "prediction": "normal", "confidence": 0.95},
                {"packet_id": 2, "prediction": "attack", "confidence": 0.87}
            ]
        }
    """
```

**验收标准**：
- API能够接收PCAP文件
- 返回正确的分析结果
- 错误处理完善
- 响应时间 < 5秒（1000个包）

**输出文件**：
```
backend/app.py（更新）
backend/tests/test_api_pcap.py
```

---

### 第三优先级：特征工程模块

#### 5. 统计特征提取
**负责人**：黄博波  
**状态**：待开始  
**预计工作量**：4小时

**任务清单**：
- [ ] 创建`backend/src/modules/features/statistical.py`
- [ ] 实现基础统计特征（均值、方差、最大值、最小值）
- [ ] 实现高级统计特征（偏度、峰度）
- [ ] 实现分布特征（百分位数）
- [ ] 单元测试

**实现方案**：
```python
# backend/src/modules/features/statistical.py
import numpy as np
from scipy import stats

class StatisticalFeatures:
    def extract(self, data):
        features = {
            'mean': np.mean(data, axis=0),
            'std': np.std(data, axis=0),
            'max': np.max(data, axis=0),
            'min': np.min(data, axis=0),
            'skewness': stats.skew(data, axis=0),
            'kurtosis': stats.kurtosis(data, axis=0),
            'percentile_25': np.percentile(data, 25, axis=0),
            'percentile_75': np.percentile(data, 75, axis=0)
        }
        return features
```

**输出文件**：
```
backend/src/modules/features/statistical.py
backend/tests/test_statistical_features.py
backend/reports/statistical_features_analysis.txt
```

---

#### 6. 时序特征提取
**负责人**：黄博波  
**状态**：待开始  
**预计工作量**：4小时

**任务清单**：
- [ ] 创建`backend/src/modules/features/temporal.py`
- [ ] 实现滑动窗口特征
- [ ] 实现时间间隔特征
- [ ] 实现趋势特征
- [ ] 单元测试

**实现方案**：
```python
# backend/src/modules/features/temporal.py
import numpy as np

class TemporalFeatures:
    def __init__(self, window_size=10):
        self.window_size = window_size
    
    def extract_window_features(self, data):
        # 滑动窗口统计
        features = []
        for i in range(len(data) - self.window_size + 1):
            window = data[i:i+self.window_size]
            features.append({
                'window_mean': np.mean(window),
                'window_std': np.std(window),
                'window_trend': self._calculate_trend(window)
            })
        return features
    
    def _calculate_trend(self, window):
        # 线性回归斜率
        x = np.arange(len(window))
        slope = np.polyfit(x, window, 1)[0]
        return slope
```

**输出文件**：
```
backend/src/modules/features/temporal.py
backend/tests/test_temporal_features.py
```

---

### 第四优先级：前端优化

#### 7. 修复前端API错误处理
**负责人**：龚斯哲  
**状态**：待开始  
**预计工作量**：2小时

**任务清单**：
- [ ] 读取`frontend/script.js`
- [ ] 添加API调用错误处理
- [ ] 添加加载状态提示
- [ ] 添加超时处理
- [ ] 测试所有API接口

**实现方案**：
```javascript
// frontend/script.js
async function callAPI(endpoint, data) {
    try {
        showLoading();
        const response = await fetch(endpoint, {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(data),
            timeout: 30000
        });
        
        if (!response.ok) {
            throw new Error(`API错误: ${response.status}`);
        }
        
        const result = await response.json();
        hideLoading();
        return result;
    } catch (error) {
        hideLoading();
        showError(`请求失败: ${error.message}`);
        console.error(error);
    }
}
```

**输出文件**：
```
frontend/script.js（更新）
```

---

#### 8. 实现PCAP上传界面
**负责人**：龚斯哲  
**状态**：待开始  
**依赖**：PCAP分析API完成  
**预计工作量**：4小时

**任务清单**：
- [ ] 设计PCAP上传界面（HTML）
- [ ] 实现文件选择功能
- [ ] 实现拖拽上传功能
- [ ] 实现上传进度条
- [ ] 实现分析结果展示
- [ ] 实现结果可视化（图表）

**界面设计**：
```html
<!-- frontend/index.html -->
<div class="pcap-upload-section">
    <h2>PCAP文件分析</h2>
    <div class="upload-area" id="uploadArea">
        <input type="file" id="pcapFile" accept=".pcap,.pcapng">
        <p>拖拽PCAP文件到此处，或点击选择文件</p>
    </div>
    <button id="analyzeBtn">开始分析</button>
    <div class="progress-bar" id="progressBar"></div>
    <div class="results" id="results"></div>
</div>
```

**验收标准**：
- 支持拖拽上传
- 显示上传进度
- 正确展示分析结果
- 响应式设计

**输出文件**：
```
frontend/index.html（更新）
frontend/style.css（更新）
frontend/script.js（更新）
```

---

## 📅 时间表（Day 4-7）

### Day 4（4月19日，今天）
- [x] 启动CNN-LSTM模型训练
- [ ] 监控训练进度
- [ ] 开始PCAP分析功能开发
- [ ] 开始特征工程模块开发

### Day 5（4月20日）
- [ ] 完成CNN-LSTM模型训练
- [ ] 完成模型融合
- [ ] 完成PCAP特征提取
- [ ] 完成统计特征模块

### Day 6（4月21日）
- [ ] 完成PCAP分析API
- [ ] 完成时序特征模块
- [ ] 完成前端API错误处理
- [ ] 开始PCAP上传界面

### Day 7（4月22日）
- [ ] 完成PCAP上传界面
- [ ] 前后端集成测试
- [ ] 修复发现的Bug
- [ ] 生成Day 4-7进度报告

---

## 🎯 关键里程碑

### 里程碑1：模型训练完成（4月20日）
**交付物**：
- ✅ RandomForest模型
- 🔄 CNN-LSTM模型
- ⏳ Ensemble融合模型
- ⏳ 模型性能报告

**验收标准**：
- 所有模型训练完成
- 模型性能达标（准确率>85%）
- 模型文件已保存

---

### 里程碑2：PCAP分析功能完成（4月21日）
**交付物**：
- ⏳ PCAP特征提取模块
- ⏳ PCAP分析API
- ⏳ 测试用例

**验收标准**：
- 能够分析PCAP文件
- API返回正确结果
- 测试通过

---

### 里程碑3：前端集成完成（4月22日）
**交付物**：
- ⏳ PCAP上传界面
- ⏳ 分析结果展示
- ⏳ 前后端集成测试报告

**验收标准**：
- 前端功能完整
- 前后端通信正常
- 用户体验良好

---

## 📊 风险管理

### 🔴 高风险

#### 风险1：CNN-LSTM训练时间过长
**影响**：可能延误后续任务  
**概率**：中等  
**缓解措施**：
- 使用GPU加速（如果可用）
- 减少训练轮数（从100轮减到50轮）
- 使用更小的batch size
- 提前准备备用方案（只使用RandomForest）

#### 风险2：PCAP特征提取难度大
**影响**：PCAP分析功能无法完成  
**概率**：中等  
**缓解措施**：
- 参考开源项目（CICFlowMeter）
- 简化特征集（只提取核心特征）
- 寻求技术支持
- 准备降级方案（手动特征提取）

---

### 🟡 中风险

#### 风险3：前后端集成问题
**影响**：系统无法正常运行  
**概率**：低  
**缓解措施**：
- 提前定义API接口规范
- 增加单元测试
- 进行集成测试

#### 风险4：团队成员进度不一致
**影响**：整体进度延误  
**概率**：中等  
**缓解措施**：
- 每日进度检查
- 及时调整任务分配
- 互相帮助解决问题

---

## 📞 沟通机制

### 每日站会（可选）
**时间**：每天晚上8:00  
**时长**：15分钟  
**内容**：
1. 今天完成了什么？
2. 遇到了什么问题？
3. 明天计划做什么？

### 进度报告
**频率**：每天  
**提交时间**：晚上9:00  
**格式**：
```
【姓名】Day X 进度报告
✅ 已完成：
- 任务1
- 任务2

⏳ 进行中：
- 任务3（预计完成时间）

❌ 遇到的问题：
- 问题描述
- 需要的帮助
```

### 紧急沟通
**渠道**：微信群  
**响应时间**：< 1小时  
**适用场景**：
- 遇到阻塞性问题
- 需要紧急协助
- 发现重大Bug

---

## 🎯 成功标准

### Day 4-7 完成标准
- [ ] CNN-LSTM模型训练完成，性能达标
- [ ] 模型融合完成，性能优于单模型
- [ ] PCAP分析功能完成，能够正确分析文件
- [ ] 特征工程模块完成，能够提取统计和时序特征
- [ ] 前端优化完成，用户体验良好
- [ ] 前后端集成测试通过
- [ ] 所有代码提交到Gitee

### 整体项目成功标准
- [ ] 系统功能完整（数据处理、模型训练、PCAP分析、前端展示）
- [ ] 模型性能达标（准确率>85%，召回率>75%）
- [ ] 文档完整（技术文档、用户手册、API文档）
- [ ] 演示成功（系统演示、答辩PPT）
- [ ] 按时交付（4月30日前完成）

---

## 📝 附录

### 技术栈总结
**后端**：
- Python 3.8+
- TensorFlow 2.21.0
- scikit-learn
- Flask
- scapy

**前端**：
- HTML5
- CSS3
- JavaScript (ES6+)
- Chart.js（可视化）

**数据集**：
- NSL-KDD（训练集125,973样本，测试集22,544样本）

**模型**：
- RandomForest（已完成）
- CNN-LSTM（训练中）
- Ensemble（待开发）

---

### 参考资源
1. **NSL-KDD数据集**：https://www.unb.ca/cic/datasets/nsl.html
2. **TensorFlow文档**：https://www.tensorflow.org/
3. **Scapy文档**：https://scapy.readthedocs.io/
4. **CICFlowMeter**：https://github.com/ahlashkari/CICFlowMeter

---

### 联系方式
- **项目负责人**：[姓名]
- **微信群**：NIDS项目冲刺群
- **Gitee仓库**：https://gitee.com/[your-repo]

---

**文档更新时间**：2026年4月16日  
**下次更新时间**：2026年4月19日（Day 5）

---

## 🚀 立即行动

### 今天晚上（4月16日）
1. **黄子权**：监控CNN-LSTM训练进度
2. **张子城**：开始学习scapy库，准备PCAP分析
3. **黄博波**：开始设计特征工程模块
4. **龚斯哲**：开始修复前端API错误处理

### 明天（4月17日）
1. **全员**：检查CNN-LSTM训练结果
2. **张子城**：开始实现PCAP特征提取
3. **黄博波**：开始实现统计特征模块
4. **龚斯哲**：完成前端错误处理，开始PCAP上传界面

---

**💪 让我们继续冲刺，按时完成项目！**
